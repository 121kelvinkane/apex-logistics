generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Shipment {
  id          String @id @unique
  origin      String
  destination String
  status      String
  progress    Int
  currentLat  Float
  currentLng  Float
}